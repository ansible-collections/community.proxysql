---
- name: "{{ role_name }} | {{ current_test }} | read from memory"
  shell: mysql -uadmin -padmin -h127.0.0.1 -P6032 -BNe "SELECT * FROM pgsql_hostgroup_attributes" | tr '\t' ','
  register: memory

- name: "{{ role_name }} | {{ current_test }} | read from disk"
  shell: mysql -uadmin -padmin -h127.0.0.1 -P6032 -BNe "SELECT * FROM disk.pgsql_hostgroup_attributes" | tr '\t' ','
  register: disk

- name: "{{ role_name }} | {{ current_test }} | read from runtime"
  shell: mysql -uadmin -padmin -h127.0.0.1 -P6032 -BNe "SELECT * FROM runtime_pgsql_hostgroup_attributes" | tr '\t' ','
  register: runtime
